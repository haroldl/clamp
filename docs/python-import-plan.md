# Python Import Implementation Plan

## Summary

Implement Python imports around first-class Clamp module objects and a `sys.modules`-style cache. Common Lisp packages are useful as isolated execution namespaces for generated module code, but they should not be the semantic model exposed to Python code.

## Design

- Represent modules as `py-module-object` values with normal Python attribute storage, `__name__`, `__package__`, `__file__`, and a hidden backing package name.
- Maintain a process-wide module cache keyed by full dotted names. Insert modules before execution and remove them if execution fails, following CPython importlib's important circular-import behavior.
- Resolve v1 source modules from the main script directory and current working directory. Support `module.py`, `package/__init__.py`, and `package/submodule.py`.
- Use Common Lisp packages only to isolate generated top-level symbols per module. Top-level assignments and function definitions also synchronize into the module object's attributes.

## Supported V1 Syntax

- `import m` and `import a.b`, including parent binding for dotted imports.
- `import m as alias` and `import a.b as alias`.
- `from m import name` and `from m import name as alias`.
- Package submodule imports when the package has `__init__.py`.

## Current Status

- Relative imports, star imports, `sys.path`, a user-visible `sys.modules`/`sys.path`/`sys.argv` module surface, function-local import bindings, and circular-import partial-initialization/cleanup behavior are covered by regression examples.
- CPython native modules can now be imported through a C API bridge, discovered by extension suffix on `sys.path`/package `__path__`, loaded by path with `importlib` extension-loader semantics, exposed as wrapped `PyObject*` values, displayed, queried for attributes, checked with native `callable()`, `isinstance()`, and `issubclass()` semantics, called with positional and keyword Clamp arguments, and used through native item get/set/delete including slices, iteration, reversed iteration, length hints, containment, context-manager enter/exit, numeric add/subtract/multiply/true-divide/floor-divide/modulo/divmod/power/unary, rich-comparison, pointer identity for repeated wrappers around the same native object, retained references for borrowed native objects crossing into Clamp, hash, length, native attribute set/delete, native `repr()`, native `round()` with `ndigits`, native `format()`/f-string format specs, native `int()`/`float()` conversion through extension dunders, native `bytes()` construction/conversion coverage, native `dir()`/`vars()` introspection, and live native module `__dict__` mutation protocols. Extension module metadata now preserves normal `__loader__ is __spec__.loader` identity, CPython-backed package parents expose imported child modules through a fallback child binding after native attribute lookup, `importlib` exposes `util` and `machinery` child modules, and `ExtensionFileLoader.create_module()`, `importlib.util.module_from_spec()` for extension specs, passive extension `find_spec()` that does not populate `sys.modules`, `importlib.machinery.PathFinder.find_spec()` for top-level and explicit-path extension lookups, `importlib.machinery.FileFinder` direct/path-hook extension lookups, `BuiltinImporter` specs/loading for registered built-in modules, `FrozenImporter` surface for declining unsupported frozen modules, `SourcelessFileLoader` construction/metadata, `.pyc` execution through `exec_module()`, `FileFinder` bytecode-suffix discovery/import, and zip archive imports with zipimport metadata plus explicit `PathFinder.find_spec()` zip path lookups, `sys.meta_path`/`sys.path_hooks`/`sys.path_importer_cache`/`sys.builtin_module_names` import-state surfaces plus common package-runtime `sys` fields such as `version`, `version_info`, `hexversion`, `executable`, prefixes, `api_version`, `platform`, and `implementation`, `PathFinder`/`FileFinder` cache invalidation and `importlib.invalidate_caches()` cache clearing, namespace package discovery/import with `NamespaceLoader`, custom `sys.meta_path` and `sys.path_hooks` finder/loader imports via `spec_from_loader()`, package resource traversal through `importlib.resources.files()`, `read_text()`/`read_binary()`, `as_file()`, legacy `open_text()`/`open_binary()`/`path()`/`is_resource()`/`contents()` helpers, and `importlib.resources.abc` ABC/protocol imports, CPython-backed `importlib.metadata` distribution/version/entry-point APIs plus public helper classes and helper submodule imports, direct `ExtensionFileLoader(name, path)` construction including extension `get_code()`/`get_source()`/`get_data()`/`is_package()` behavior, catchable loader name mismatch `ImportError`, path-like `spec_from_file_location()` inputs, machinery suffix constants/`all_suffixes()`, extension-aware `spec_from_file_location()`, extension package (`__init__.<extension>`) spec/discovery metadata, CPython-style source cache path helpers/`__cached__` metadata, `importlib.util.resolve_name()`, CPython-backed `source_hash()`, `MAGIC_NUMBER`, `decode_source()`, private `_find_spec()` compatibility, machinery bytecode suffix lists, `WindowsRegistryFinder` compatibility surface, Clamp-native `pkgutil` discovery/loader/data/namespace-extension helpers over Clamp finders, and `LazyLoader` construction/factory delegation, and native extension reload/cache behavior have regression coverage. Broader native import coverage spans `_json`, `_decimal`, `_hashlib`, `_bz2`, `_zoneinfo`, `_sqlite3`, `markupsafe._speedups`, `PIL._imaging`, `lxml.etree`, `cryptography.hazmat.bindings._rust`, and `cryptography.hazmat.primitives.hashes` import, hash algorithm metadata, native `HashAlgorithm` identity checks, and Rust-backed `Hash(hashes.SHA256())` digest use in local probes/regression examples.
- Common CPython scalar results from native calls and attributes (`None`, `bool`, `int`, `float`, `str`, and `bytes`) are converted back to normal Clamp values, common Clamp containers (`list`, `tuple`, and `dict`) are converted in both directions for native calls and returns, Clamp `bytes` are converted to CPython `bytes` for native calls, and native custom exceptions can be raised, caught by their native class, and inspected with native `type()`. Clamp source compatibility now also covers inherited class definitions, class annotations, top-level annotated assignments, Ellipsis constants, PEP 604 `|` over native typing objects, keyword-only/vararg/kwargs function signatures, `*args`/`**kwargs` call expansion, synchronous generator functions, native scalar/set/frozenset builtin type objects for annotation aliases, module-local shadowing so imports such as `Callable` do not overwrite shared builtins, and CPython callback proxies for Clamp callables can receive keyword arguments.
- The local `pydantic-core` package imports through its normal `pydantic_core._pydantic_core` extension module and exposes callable native classes such as `SchemaValidator`. Clamp now includes targeted built-in compatibility modules for the pydantic dependency surface (`typing_extensions`, `typing_inspection`, and `annotated_types`) plus a CPython-side `typing_extensions.Sentinel` fallback for extension-internal imports when the host package is older. Against `~/local/pydantic-main`, a minimal unshimmed `BaseModel` subclass can be constructed, instantiated, validated through pydantic-core, dumped with `model_dump()`, serialized with `model_dump_json()`, and can catch/call through native `ValidationError` objects. Richer local probes now also cover nested models, list fields, field aliases with case-preserving keyword flow, computed fields, field serializers, field validators, model validators, `RootModel[list[int]]`, `TypeAdapter` validation/dumping/schema generation, `ConfigDict(populate_by_name=True)`, `model_json_schema()` generation, stdlib and pydantic dataclass validation including pydantic-core writes through native proxy-backed instances, `Field(default_factory=...)`, `PrivateAttr`, `model_copy()`, `copy.copy()`/`copy.deepcopy()` on pydantic models, and `validate_call` invoking a Clamp function through pydantic-core with keyword arguments plus default-aware signature validation errors.
- Pydantic-driven source/runtime compatibility now additionally covers lambda expressions, named expressions including walrus targets across function block mutation scopes, `yield from`, starred list/tuple/set displays, starred assignment targets, chained assignment, nested classes in class bodies, mixed-case function bindings, function/method `__name__` metadata, `functools.wraps`-style callable wrapper metadata including `__wrapped__`, writable callable attributes, class-body decorators, `staticmethod`, minimal `property` including `__isabstractmethod__`, minimal `super`, `type.__new__`, explicit/inherited metaclass class creation, dynamic metaclass calls, CPython metaclass `__new__` calls with the explicit metaclass argument, CPython `abc.abstractmethod` marking Clamp callables, nested-yield generator detection, dict unpack displays, `zip(strict=...)`, `__mro_entries__` resolution for typing aliases in class bases, parameterized generic Pydantic models, `RecursionError`, `operator.attrgetter`, `inspect.signature()` parameter defaults/annotations for Clamp functions, callable `__dict__`/`__defaults__`/`__kwdefaults__` metadata, `inspect.unwrap()` over `__wrapped__` chains, shallow `inspect.Parameter` constants, Clamp function recognition as CPython `types.FunctionType` for native/Pydantic type checks, shallow `dataclasses`/`functools` compatibility modules, `TypedDict`-style dict factories, and a narrow PEP 604 type-union expression fallback for native typing interop.
- Remaining import work is now deeper compatibility: broader Pydantic behavior beyond the currently probed model validation/dump/serialization/schema paths, including broader generic/model edge cases beyond the current parameterized model and `RootModel` probes, full dataclasses/functools/inspect behavior beyond the currently covered dataclass markers, `fields()`/`asdict()` coverage, model copy paths, and pydantic dataclass validation basics, remaining native extension class identity/protocol gaps beyond the currently covered native ABC-base proxying, and broader CPython extension object protocols beyond the currently bridged call, attribute, item/slice, iteration/reversed iteration, containment, bytes, numeric operations, rich-comparison, pointer identity, borrowed-reference retention, hash, length, native attribute mutation, native repr/introspection, native round delegation, and live module dictionary operations.

## Deferred Work

- Broader native object protocol coverage, full extension-module reference lifetime auditing, and replacing targeted Pydantic compatibility shims with fuller dependency/package compatibility where needed.
- More of the full `sys` module API beyond `modules`, `path`, and `argv`.

## Validation

Add examples covering plain imports, aliases, from-import, package submodules, and repeated imports executing once. Add circular-import examples before expanding import semantics further. Keep existing examples passing.
